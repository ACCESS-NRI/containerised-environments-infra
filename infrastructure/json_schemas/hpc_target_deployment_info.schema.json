{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "hpc_target_deployment_info.schema.json",
  "type": "object",
  "required": ["name", "deployments"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Name of the HPC target for the deployment (e.g. Gadi, Setonix)"
    },
    "deployments": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
            "module_name", 
            "module_version", 
            "module_type", 
            "deployment_stage", 
            "module_usage_instructions", 
            "env_lock",
            "started_at",
            "completed_at",
            "success"
        ],
        "additionalProperties": false,
        "properties": {
          "module_name": { "type": "string", "minLength": 1 },
          "module_version": { "type": "string", "minLength": 1 },
          "module_type": {
            "type": "string",
            "enum": ["DEVELOPMENT", "STABLE"]
          },
          "deployment_stage": {
            "type": "string",
            "enum": ["STAGING", "PRODUCTION"]
          },
          "module_usage_instructions": { "type": "string" },
          "env_lock": { "type": "string" },
          "started_at": { "type": "string", "minLength": 1 },
          "completed_at": { "type": "string" },
          "success": {
            "type": "string",
            "enum": ["true", "false", ""]
          }
        }
      }
    }
  }
}